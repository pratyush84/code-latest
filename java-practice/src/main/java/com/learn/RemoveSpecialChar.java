package com.learn;

import java.util.ArrayList;
import java.util.List;

public class RemoveSpecialChar {
    public static void main(String[] args) {
//        System.out.println(remove("ab#",'#',""));
    }
/*
    static String remove(String str, char chr, String prefix){
        List<String> returnList = new ArrayList<>();
        if(str.length() == 0){
            returnList.add(prefix);
            return returnList;
        }

        char firstChar = str.charAt(0);

        if(firstChar == chr){
            returnList.add() remove(str.substring(1),chr,prefix);
            remove(str.substring(1),chr,str.substring(0,str.length()-1));
        } else{

            return remove(str.substring(1),chr,prefix.concat(String.valueOf(firstChar)));
        }
    }*/
}
